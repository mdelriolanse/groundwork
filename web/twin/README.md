# VFLab twin

```bash
python3 ../../data/vlft/compose_scene.py   # regenerate scene.json
python3 -m http.server 8765
```

Click a station to **Focus** it (hides the rest). Empty click shows the cell. `?asset=RPP1` deep-links. Hero `RPP1` / `URjoint1` → `cwru:105.mat`.

Geometry: VLFT CC BY-NC 4.0. See `data/vlft/ATTRIBUTION.md`. Warehouse shell is omitted (occludes the line).
